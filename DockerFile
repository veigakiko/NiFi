FROM apache/nifi:latest

# Expor as portas
EXPOSE 8080 8443

# Listar os arquivos para verificar a estrutura do diretório
RUN ls -l bin

# Comando para iniciar o NiFi
CMD ["bin/nifi.sh", "run"]
